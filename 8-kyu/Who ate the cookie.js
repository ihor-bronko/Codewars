// Who ate the cookie?

// function cookie(x){
// if(x.indexOf(String)) return "Who ate the last cookie? It was Monica!"
// if(x === Number) return "Who ate the last cookie? It was Zach!"
// if(x.indexOf(Boolean)) return "Who ate the last cookie? It was the dog!"
//
// }

const cookie = x => `Who ate the last cookie? It was ${{string:'Zach', number:'Monica'}[typeof x] || 'the dog'}!`



